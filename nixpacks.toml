[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install -g pnpm@10 && pnpm install"]

[phases.build]
cmds = ["rm -rf node_modules/.cache dist && pnpm run build"]

[start]
cmd = "node dist/standalone.js"
