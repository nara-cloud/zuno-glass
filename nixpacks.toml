[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["echo 'usando build pré-compilado'"]

[phases.build]
cmds = ["echo 'dist já compilado, pulando build'"]

[start]
cmd = "node dist/standalone.js"
